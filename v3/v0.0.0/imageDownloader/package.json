{
  "name": "image-downloader-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that downloads images from URLs and hosts them on R2",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.21.2"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "sharp": "^0.33.0"
  }
}
